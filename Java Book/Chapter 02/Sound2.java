/*
    Compute the distance to a wall
    using the speed of sound as a
    means of measuring the distance
*/
class Sound2 {
    public static void main(String args[]) {
        double dist;

        dist = (7.2 * 1100) / 2;
        System.out.println("Distance to wall: " + dist);
    }
}
